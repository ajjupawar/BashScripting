name: Run bash script 001 Test

on:
  push:
    branches:
      - main

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run existing Bash script
        run: ./001_variables.sh 
